#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 1010;
int a[N][N];
int n, m, q;

int lower(int x) {
  return x & (-x);
}

int get(int x) {
  int res = 0;
  for(; x; x -= lower(x)) res++;
  return res;
}

ll ask(int x, int y) {
  
}

int main() {
  scanf("%d%d", &n, &m, &q);
  for(int i = 1; i <= n; ++i) 
    for(int j = 1; j <= n; ++j)
      scanf("%1d", &a[i][j]);
  
  return 0;
}
