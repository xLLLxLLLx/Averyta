#include <bits/stdc++.h>
using namespace std;

int main() {
  srand(time(NULL));
  int n = 1e5;
  printf("%d %d\n", n, n - 1);
  for(int i = 2; i <= n; ++i) {
    int x = rand() % (i - 1) + 1;
    printf("%d %d\n", x, i);
  }
  return 0;
}
